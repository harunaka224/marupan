<%= form_with model: [post, post_comment], local: false do |f| %>
  <%= f.text_area :comment, rows: '5', placeholder: "コメント入力", class: "w-100 comment-form" %>
  <div class="text-right"><%= f.submit "送信", class: "btn btn-dark btn-sm"%></div>
<% end %>